<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mm.dao.WxMemberDao">

    <!-- 根据 会员昵称 查询 会员信息 -->
    <select id="findByNickName" parameterType="string" resultType="WxMember">
        select * from t_wx_member where nickName = #{nickName}
    </select>

    <!-- 添加会员 -->
    <insert id="addWxMember" parameterType="WxMember">
        insert into t_wx_member
        (nickName, avatarUrl, gender, city, province, country, language, createTime)
        values
        (#{nickName}, #{avatarUrl}, #{gender}, #{city}, #{province}, #{country}, #{language}, #{createTime})
        <selectKey keyColumn="id" keyProperty="id" resultType="int" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <!-- 通过 id 查询对应 wxMember -->
    <select id="findById" resultType="WxMember" parameterType="int">
        select * from t_wx_member where id = #{id}
    </select>

    <!-- 更新 wxMember -->
    <!--
        1. 在做 update table 表中某一个字段时，要考虑是否以后其他字段也会需要修改
        如果也需要修改，那就直接在此次 update 语句中全部添加进去

   PS: 如果 update table 时，哪个字段的值没有发生变化，sql 是不需要修改该值的，只会修改有变化的值，所以不需要担心每次都会对所有字段执行 update ，造成 sql 臃肿.
    -->
    <update id="update" parameterType="WxMember">
        update t_wx_member
        set nickName = #{nickName}, avatarUrl = #{avatarUrl}, gender = #{gender}, city = #{city}
        province = #{province}, country = #{country}, language = #{language}, cityId = #{cityId}, courseId = #{courseId}
        lastCategoryKind = #{lastCategoryKind}, lastCategoryType = #{lastCategoryType}, lastCategoryId = #{lastCategoryId}, lastQuestionId = #{lastQuestionId}
        where id = #{id}
    </update>

</mapper>
